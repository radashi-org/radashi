---
title: isInt
description: 'Determine if a value is an int'
group: Typed
---

## Basic usage

Pass in a value and get a boolean telling you if the value is an int.

```ts
import * as _ from 'radashi'

_.isInt(12) // => true
_.isInt(12.233) // => false
_.isInt('hello') // => false
```
